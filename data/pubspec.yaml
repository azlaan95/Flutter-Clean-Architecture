name: azl_data
description: Clean Data Layer

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=2.19.5 <3.0.0'

dependencies:
  http: ^0.13.5
  shared_preferences: ^2.0.20

dev_dependencies:
  flutter_lints: ^2.0.1
  build_runner: ^2.3.3
